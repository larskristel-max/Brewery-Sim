{
  "name": "brewery-sim",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "dev": "npm run build && node scripts/dev-server.mjs",
    "test": "npm run build && node scripts/run-tests.mjs"
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}
